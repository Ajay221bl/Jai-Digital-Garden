---
{"dg-publish":true,"permalink":"/notes/overfitted-model/","tags":["statistics","probability"]}
---

# overfitted model
an overfitted model is overly tailored to the data it was trained on, capturing noise rather than general patterns. As a result, it may perform poorly when applied to new data, failing to generalize effectively.
